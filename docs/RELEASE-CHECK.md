# Проверка перед демонстрацией

Дата: 23 сентября 2026. Публичная демонстрация: https://jarvis-ekt.vercel.app.

## Первая проверка (версия 867ed71)

- 25 модульных тестов: все прошли.
- 12 HTTP-сценариев локально и те же 12 на Vercel: все прошли, тестовая корзина очищена.
- Браузер: выбор товара фотографией, предложение и подтверждение, корзина после перезагрузки, удаление, RU/KK и поиск артикула прошли.
- XLSX и DOCX распознаются локально и на Vercel. Локально также проверены PDF, TXT, отказ для поврежденного PDF и файла больше 3 МБ.
- Локальная production-сборка и первый облачный build успешны.
- Первый публичный PDF-тест выявил неполный серверный пакет: добавлен pdf-parse/worker и явное включение native canvas/worker.
- Мобильный тест нашёл 6px горизонтального переполнения от canvas кнопки: добавлен overflow-x:clip шапки.

## Соответствие заданию

| Must have из PDF | Статус и доказательство |
|---|---|
| Наличие, характеристики, сертификаты | Реальный API; товар 515291, остаток 23, цена 64920 ₸ на момент проверки; конфликт 160/250А отмечен. Если сертификат не предоставлен, это явно сообщается. |
| Аналог отсутствующего товара | 18412 имеет нулевой остаток; найдены 21449, 19281, 515247 с совпадением пяти критических параметров. При недоступности API карточки явно помечены датированным снимком. |
| Условия покупки | RU/KK ответы с официальным источником, оплатой, доставкой и кратностью. |
| Корзина только с подтверждением | Проверены read-only proposal, отказ без true, проверка остатка, защита от повторного токена и чужого Origin. |
| Прямая ссылка на корзину | /cart, состояние текущего браузера сохраняется после перезагрузки. |

## Что важно для оценки

По положению: ценность 25, качество 20, инновационность 15, масштабирование 20, презентация/демо/ответы 20. Это веса критериев, а не набранные командой баллы. Сценарий демонстрации и аргументы — в JUDGING.md. README содержит самостоятельный запуск, архитектуру, зависимости и переменные окружения (п. 5.4.15).

Ограничения: отдельный прототип, не установлен на действующий ekt.kz; настоящие API корзины и оформления заказа партнёром не предоставлены. Ссылка /cart привязана к текущему браузеру. Сканы PDF без текстового слоя нужно загрузить изображением. Ответы зависят от API: в одном локальном тесте было 24,8 с; на Vercel тот же сценарий занял 6,2 с. Уровень точности, экономия и гарантированный итоговый балл не заявляются.

## Подтверждение после повторного деплоя

Версия приложения 867ed71, production READY. На публичном URL отсутствуют hero-coordinate и hero-corner. PDF после исправления: HTTP 200, 4555 символов, 1,610 с. Локальная мобильная проверка: viewport 390px, document scrollWidth 390px.

## Усиление продукта — 15:36, UTC+5

Версия 1393ebf опубликована на Vercel и в GitHub. Проверено:

- **31/31** модульный тест, TypeScript и production-сборка без ошибок.
- **12/12** HTTP-проверок повторно прошли на публичном адресе. RU-ответ 6,104 с, KK-условия 2,104 с, проверка инструкций документа 5,505 с — отдельные замеры, не SLA.
- `npm run test:browser`: два сквозных сценария с 17 контрольными точками прошли локально и на Vercel. Включены загрузка спецификации, ненайденный артикул, редактирование количества, пакет из двух товаров с подтверждением, скачивание TXT, сравнение и фильтр отличий, мобильная ширина и матовый чат. Необработанных клиентских ошибок в этих сценариях не было.
- Реальное фото Legrand DRX250: на Vercel ответ 3,614 с, кандидаты 515288 и 515291 нужной серии и номинала по названию. Источник с конфликтом 160/250А отмечен. Мелкая маркировка Uimp в разных прогонах прочитана неодинаково; точность OCR не считается доказанной. Дополнительная правка 82e6e9a обозначает чтение как предварительное и требует сверки с фото.
- Реальная отправка тестовой спецификации на Vercel: 5,399 с; два точных соответствия и один неизвестный артикул без выдуманной замены. Это проверка небольшого примера, не доказательство обработки произвольных файлов.
- Проверка мобильного чата: ширина панели 370px при viewport 390px, `backdrop-filter: blur(24px)`, горизонтального переполнения нет. Нижняя подложка прозрачна.
- `npm install` сообщил 0 известных уязвимостей установленного дерева на момент выполнения.

Инструкция для участника: [USER-TEST.md](USER-TEST.md). Разбор критериев и оставшихся задач: [PRODUCT-UPGRADE.md](PRODUCT-UPGRADE.md).

Финальная публикация: **82e6e9a**, Vercel **READY**, deployment `dpl_HsVbPp2x2YWwfXGqAxpTh4yxUBUG`. После переключения публичного адреса отдельно проверены новая подпись предварительной маркировки, кандидаты 515288/515291 и доступность примера спецификации (HTTP 200). Этот коммит меняет только подпись чтения фото относительно полного прогона 1393ebf.
