# Проверка перед демонстрацией

## Финальный production — 23.09.2026, 17:27

- Код приложения **`30064d0`**, отправлен в `main` репозитория организатора. Vercel завершил production build и TypeScript без ошибок, статус **READY**.
- Основной адрес: **https://jarvis-ekt.vercel.app**. Неизменяемый deployment: https://jarvis-duslvw2d6-sem-consult.vercel.app, ID `dpl_9YgQfLdP4VpMwkqpHJskGNg7iB4z`.
- Локальная production-сборка успешна. **115/115** модульных проверок и все семь стандартных браузерных сценариев прошли. Прогон настоящего Neon локально: A → новый B → восстановление A/B → reload → удаление двух своих тестовых записей → выход; остаток синтетических записей 0. Только ответ ассистента в этом отдельном тесте подменён.
- На итоговом публичном адресе **12/12 HTTP-проверок** прошли: живой каталог, противоречие свойств, неизменность корзины до подтверждения, отклонение неправильного подтверждения, повтор токена, контроль остатка, чужой Origin, изменение/удаление, RU/KK и изоляция инструкций во вложении. Тестовая корзина пуста. Отдельные ответы: RU 2,711с, KK 2,388с, вложение 6,665с; это измерения, не гарантированный SLA.
- Публичный footer: Chrome, 1440/390px × RU/KK, четыре режима — корректные размеры текста, 11 ссылок, переполнение 0, ошибок браузера 0.
- Публичный `browser-product.cjs` прошёл все шесть проверок: спецификация с неизвестным артикулом, пакетное подтверждение корзины, TXT, сравнение на телефоне, матовый чат и отсутствие ошибок клиентского выполнения.
- **Публичная история и демо-вход также PASS:** настоящие Neon Auth/Postgres, `user1` → A → «Новый» → B → восстановить A → восстановить B → reload → новый → восстановить A. Оба несохранённых ранее чата автоматически попали в историю. Удалены только две созданные тестом записи; выход вернул гостевой 401. Остаток синтетических записей 0, писем 0. Только ответ `/api/chat` подменён для изоляции сценария истории.

## 23.09.2026, 17:24 — итоговые исправления истории и читаемости

- **115/115 модульных проверок**: в том числе сохранение предупреждений об обрезке вложений и маскирование распознаваемых платёжных реквизитов до AI/истории, без порчи артикулов и характеристик.
- «Новый диалог» и открытие прежнего сначала сохраняют текущий чат авторизованного пользователя. Проверены A → новый B → возврат A, сохранение B, ожидание незавершённого автосохранения, последняя реплика, отсутствие пустых записей, отказ переключения при ошибке, восстановление ID после reload и выход после ошибки. Этот браузерный регрессионный сценарий использует подмену auth/history API; реальный Neon проверяется отдельно.
- При конфликте ревизий можно явно сохранить отдельную копию, не перезаписывая изменения другой вкладки. Сессионный лимит приведён к 100 сообщениям, как у истории.
- Footer: основной текст 17px, ссылки 16px, заголовки 19px, подписи 14px; повышен контраст. RU/KK на 1440 и 390px — без горизонтального переполнения.
- Пользователь подтвердил на публичной версии получение настоящего кода восстановления на почту и последующий успешный вход по паролю. Доставка новой регистрации на всех почтовых сервисах этим не доказывается.
- README обновлён: демо-ссылка и общий логин в начале, актуальная история, приватность, инструкция запуска и границы интеграции. Сверка с положением и ТЗ — [JUDGE-AUDIT.md](JUDGE-AUDIT.md).
- Предыдущий production `484aa36` опубликован успешно; публичные product/account/auth-flow проверки прошли. Финальный deployment и результаты его проверки записываются следующим пунктом после готовности.

## 23.09.2026, 17:03 — регистрация и демоаккаунт, проверки перед публикацией

- **105/105** модульных проверок. Семь сценариев `npm run test:browser` прошли локально: основной путь, спецификация/сравнение, обработка ошибок, гостевой аккаунт, состояние истории, новый auth flow и CodeSlots с обычной/уменьшенной анимацией.
- Исправлена нестабильность публичного разбора таблицы `Артикул;Количество`: AI иногда оставлял количество внутри поискового запроса. Явные колонки теперь обрабатываются детерминированно; неизвестный артикул не подменяется похожим товаром.
- Neon настроен на регистрацию email+пароль, обязательную проверку почты кодом и вход паролем. Добавлен доверенный production-домен, название писем — Jarvis EKT. История требует `emailVerified === true` на сервере.
- `browser-auth-flow.cjs` проверяет реальный React/SDK с перехваченными ответами провайдера: регистрация, неверный код и повтор, пауза перед новой отправкой, подтверждение → отдельный вход паролем, установка пароля для прежнего OTP-аккаунта. Этот тест **не подтверждает доставку настоящего письма**.
- Создан отдельный общий демоаккаунт `user1 / password123`, роль `user`, без личных данных. Seed идемпотентен, существующие аккаунты не меняет. Реальные публичные sign-in/get-session/history/sign-out вернули 200; токены и хеши не выводились.
- `browser-demo-account.cjs` прошёл локально с **настоящими Neon Auth и Postgres**: вход псевдонимом, явное сохранение, reload, новый диалог, восстановление, удаление только созданной тестовой записи, выход и гостевой 401. Ответ ассистента подменён для изоляции теста; почта не отправлялась. Остаток синтетических записей — 0.
- Этот прогон выявил и исправил `INVALID_ORIGIN` в истории на localhost: Next подставлял адрес привязки `0.0.0.0`. Проверка использует протокол и Host запроса, отклоняет чужой Origin и cross-site.
- Локальный auth proxy блокирует смену реквизитов демоаккаунта и запросы кодов для него, ограничивает JSON 32 КБ, проверяет Origin и возвращает `no-store`. Прямые маршруты внешнего провайдера не объявляются закрытыми нашим proxy.
- Публичная версия `d195728` ранее прошла 12 HTTP-проверок, TXT/DOCX/XLSX/PDF, пустое изображение, настоящий vision-запрос (4,862 с), футер и мобильную корзину. Новый auth UI и исправление спецификации публикуются следующим деплоем; его результаты фиксируются отдельно.

Для повторения реального демо-теста после seed и включения флага: `node tests/browser-demo-account.cjs` с нужным `TEST_BASE_URL`. Для личной почты завершение регистрации/восстановления проверяет владелец адреса, не передавая код или пароль ассистенту.

## Доработка 23.09.2026, 16:31 — локальные результаты перед публикацией

- **91/91 модульных проверок**, успешная production-сборка и TypeScript: контекст RU/KK, точные артикулы, количество, сертификаты, сопутствующие товары, пустые изображения, лимиты документов, история и размер запроса с полными вложениями.
- **12/12 HTTP-сценариев** на локальной production-сборке: настоящий каталог, подтверждение/отмена, повтор токена, остаток, чужой Origin, языки и инструкции во вложении. Тестовая корзина очищена.
- **Пять браузерных сценариев** прошли на локальной production-сборке: основной путь, спецификация/сравнение, обработка ошибок/менеджер, гостевой доступ и формы аккаунта, состояние истории.
- `browser-reliability.cjs` воспроизводит 503 чата и просроченное предложение подменой соответствующих ответов; каталог, загрузка и применение корзины реальные. Запрос менеджеру скачивается/копируется, но не отправляется.
- `browser-account.cjs` проверяет настоящие гостевые маршруты: GET/POST/DELETE истории возвращают 401 с `no-store`. RU/KK формы входа/регистрации, обязательные поля, открытие и закрытие проверены. Auth POST не отправляются.
- `browser-history.cjs` проверяет реальный React UI с подменой auth/history/chat API: явное первое сохранение, ID/ревизии автосохранения, новый диалог, восстановление карточек и `/cart`, подтверждение удаления, очистку после выхода. Это **не проверка настоящего входа**.
- `scripts/history-check.ts`: восемь проверок реального Neon Postgres, включая запрет чтения/записи/удаления чужой записи, конфликт ревизии и удаление. Синтетическая запись очищена.
- Сквозной прогон TXT/DOCX/XLSX/PDF успешен; спецификация — 3,3–4,0 с, читаемое фото Legrand — 4,3 с. Пустая серая картинка отвергается до модели. Спецификация из 13 строк вернула 12 с предупреждением, но заняла 18,2–18,6 с: SLA «единицы секунд» для всех запросов не подтверждён.
- Точный артикул/контекст в отдельных замерах — 0,09–0,6 с без OpenAI; это измерения конкретных запросов. Политики обновлены по официальному `/checkout-delivery/`.
- Корзина получила матовые карточки с размытием 24px. Ширина 390px без горизонтального переполнения. Низ главной содержит проверенные категории и ссылки EKT.
- `npm ci --dry-run --ignore-scripts` прошёл; npm сообщает 0 известных уязвимостей, но предупреждает о peer-зависимостях beta SDK Neon. Production-сборка и использованные формы работают в проверенной конфигурации.

Завершение входа через личную почту/код требует действия пользователя и отдельно не отмечается как автоматически проверенное. Автоперенос в EKT не реализован: [причина и контракт будущей интеграции](EKT-CART-INTEGRATION.md).

Дата: 23 сентября 2026. Публичная демонстрация: https://jarvis-ekt.vercel.app.

## Первая проверка (версия 867ed71)

- 25 модульных тестов: все прошли.
- 12 HTTP-сценариев локально и те же 12 на Vercel: все прошли, тестовая корзина очищена.
- Браузер: выбор товара фотографией, предложение и подтверждение, корзина после перезагрузки, удаление, RU/KK и поиск артикула прошли.
- XLSX и DOCX распознаются локально и на Vercel. Локально также проверены PDF, TXT, отказ для поврежденного PDF и файла больше 3 МБ.
- Локальная production-сборка и первый облачный build успешны.
- Первый публичный PDF-тест выявил неполный серверный пакет: добавлен pdf-parse/worker и явное включение native canvas/worker.
- Мобильный тест нашёл 6px горизонтального переполнения от canvas кнопки: добавлен overflow-x:clip шапки.

## Соответствие заданию

| Must have из PDF | Статус и доказательство |
|---|---|
| Наличие, характеристики, сертификаты | Реальный API; товар 515291, остаток 23, цена 64920 ₸ на момент проверки; конфликт 160/250А отмечен. Если сертификат не предоставлен, это явно сообщается. |
| Аналог отсутствующего товара | 18412 имеет нулевой остаток; найдены 21449, 19281, 515247 с совпадением пяти критических параметров. При недоступности API карточки явно помечены датированным снимком. |
| Условия покупки | RU/KK ответы с официальным источником, оплатой, доставкой и кратностью. |
| Корзина только с подтверждением | Проверены read-only proposal, отказ без true, проверка остатка, защита от повторного токена и чужого Origin. |
| Прямая ссылка на корзину | /cart, состояние текущего браузера сохраняется после перезагрузки. |

## Что важно для оценки

По положению: ценность 25, качество 20, инновационность 15, масштабирование 20, презентация/демо/ответы 20. Это веса критериев, а не набранные командой баллы. Сценарий демонстрации и аргументы — в JUDGING.md. README содержит самостоятельный запуск, архитектуру, зависимости и переменные окружения (п. 5.4.15).

Ограничения: отдельный прототип, не установлен на действующий ekt.kz; настоящие API корзины и оформления заказа партнёром не предоставлены. Ссылка /cart привязана к текущему браузеру. Сканы PDF без текстового слоя нужно загрузить изображением. Ответы зависят от API: в одном локальном тесте было 24,8 с; на Vercel тот же сценарий занял 6,2 с. Уровень точности, экономия и гарантированный итоговый балл не заявляются.

## Подтверждение после повторного деплоя

Версия приложения 867ed71, production READY. На публичном URL отсутствуют hero-coordinate и hero-corner. PDF после исправления: HTTP 200, 4555 символов, 1,610 с. Локальная мобильная проверка: viewport 390px, document scrollWidth 390px.

## Усиление продукта — 15:36, UTC+5

Версия 1393ebf опубликована на Vercel и в GitHub. Проверено:

- **31/31** модульный тест, TypeScript и production-сборка без ошибок.
- **12/12** HTTP-проверок повторно прошли на публичном адресе. RU-ответ 6,104 с, KK-условия 2,104 с, проверка инструкций документа 5,505 с — отдельные замеры, не SLA.
- `npm run test:browser`: два сквозных сценария с 17 контрольными точками прошли локально и на Vercel. Включены загрузка спецификации, ненайденный артикул, редактирование количества, пакет из двух товаров с подтверждением, скачивание TXT, сравнение и фильтр отличий, мобильная ширина и матовый чат. Необработанных клиентских ошибок в этих сценариях не было.
- Реальное фото Legrand DRX250: на Vercel ответ 3,614 с, кандидаты 515288 и 515291 нужной серии и номинала по названию. Источник с конфликтом 160/250А отмечен. Мелкая маркировка Uimp в разных прогонах прочитана неодинаково; точность OCR не считается доказанной. Дополнительная правка 82e6e9a обозначает чтение как предварительное и требует сверки с фото.
- Реальная отправка тестовой спецификации на Vercel: 5,399 с; два точных соответствия и один неизвестный артикул без выдуманной замены. Это проверка небольшого примера, не доказательство обработки произвольных файлов.
- Проверка мобильного чата: ширина панели 370px при viewport 390px, `backdrop-filter: blur(24px)`, горизонтального переполнения нет. Нижняя подложка прозрачна.
- `npm install` сообщил 0 известных уязвимостей установленного дерева на момент выполнения.

Инструкция для участника: [USER-TEST.md](USER-TEST.md). Разбор критериев и оставшихся задач: [PRODUCT-UPGRADE.md](PRODUCT-UPGRADE.md).

Финальная публикация: **82e6e9a**, Vercel **READY**, deployment `dpl_HsVbPp2x2YWwfXGqAxpTh4yxUBUG`. После переключения публичного адреса отдельно проверены новая подпись предварительной маркировки, кандидаты 515288/515291 и доступность примера спецификации (HTTP 200). Этот коммит меняет только подпись чтения фото относительно полного прогона 1393ebf.
