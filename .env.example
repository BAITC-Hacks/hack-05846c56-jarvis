EKT_API_URL=https://ekt.kz/api
EKT_API_USERNAME=
EKT_API_PASSWORD=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CART_SECRET=
# Optional: accounts and saved conversations (Neon integration).
DATABASE_URL=
NEON_AUTH_BASE_URL=
# Generate a separate random secret with the command above (at least 32 characters).
NEON_AUTH_COOKIE_SECRET=
# Enable only after provisioning the separate shared demo account.
NEXT_PUBLIC_DEMO_LOGIN_ENABLED=false
